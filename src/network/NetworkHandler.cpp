#include "network/NetworkHandler.hpp"

namespace mcpi::network {

// Intentionally empty translation unit. Concrete client/server handlers will
// only be added when runtime reachability and packet semantics are evidenced.

} // namespace mcpi::network
